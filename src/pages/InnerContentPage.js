import React from 'react';
import InnerContent from '../components/InnerContent';

const InnerContentPage = (props) => {
    return (
        <div>
            <InnerContent {...props} />
        </div>
    );
};

export default InnerContentPage;